{
	"name": "webrtc-backend",
	"version": "1.0.0",
	"author": "Nav",
	"main": "src/server.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon --watch 'backend/**/*.ts' --exec 'ts-node' ./src/server.ts",
		"build": "tsc"
	},
	"dependencies": {
		"dotenv": "^16.4.7",
		"express": "^4.21.2",
		"socket.io": "^4.8.1"
	},
	"devDependencies": {
		"@types/express": "^5.0.0",
		"@types/node": "^22.10.2",
		"nodemon": "^3.1.10",
		"ts-node": "^10.9.2",
		"typescript": "^5.8.3"
	}
}
